
#include<stdio.h>
int main()
{
     int prime(int n)
     void main ()
     {
        int i,stp,edp,rst,sum=0;
        printf ("Enter Starting and Ending point ::\n");
        scanf ("%d %d",&stp,&edp);
        for (i=stp;i<=edp;i++)
        {
            rlt=prime(num);
            
        }
        sum=sum+rlt;
        printf ("%d",&sum);
     }
}