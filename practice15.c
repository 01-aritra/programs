// prime factorial of a number


#include<stdio.h>
void main()
{
   int i ,n, fact =1;
   printf ("Enter number to cal culate it's prime factorial \n");
   scanf ("%d",&n);
   for (i=1;i<=n;i++)
   {
    fact =fact*i;
   }
   if (fact ==1 && fact == num)
   {
    printf ("prime fac")
   }
}