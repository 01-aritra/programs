#include <stdio.h>
#include<stdlib.h>
#define v 6    //Number of vertices in the graph 

int ford_fulk(int graph[v][v],int source ,int sink)
{
    int maxFlow = 0;
    
    int residualGraph[v][v];
    for (int i = 0;i < v;i++)
    {
        for ()
    }

}