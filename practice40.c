/*Write a C program that reads a string entered by the user and then counts the number of 
vowels in the string using pointers. The program should prompt the user to enter a string, compute the number of
 vowels in the string using pointers, and then display the number of vowels back to the user
 .*/
 
 #include<stdio.h>
 void vow(char *str)
 {
    int count=0;
    int len = strlen(str);
    for(int i=0;i<=len;i++)
    {
        if ()
    }
 }