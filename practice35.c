// removing a numer from certain index and incerting a number.

#include<stdio.h>
void main()
{
    int sz,i;
    printf ("enter the size of array");
    scanf ("%d"&sz);
    int ar[sz],br[sz+1];
    printf("Enter the value of array");
    for (i=0;i<=n;i++)
    {
        
    }
}