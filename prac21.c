/* Write a C program that prompts the user to enter the maximum number of elements for an integer array, 
dynamically allocates memory for the array based on this maximum size, and then asks the user to input the 
elements of the array. Finally, the program should print the sum of all the elements in the array."*/

#include<stdio.h>
#include<stdlib.h>

int main()
{
    int maxar_size;
    printf("Enter the maximum number of elements for an integer array. :");
    scanf("%d",&maxar_size);
    
    
    return 0;
}