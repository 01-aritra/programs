/* Write a function char* longestCommonSubstring(char** arr, int n) that finds the longest common substring 
among an array of strings. The function should compare characters to find the longest substring that is common 
to all strings in the array.*/

#include<stdio.h>
#include<stdlib.h>

char* longestCommonSubstring(char** arr, int n)
{
    
}