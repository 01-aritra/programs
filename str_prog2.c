//Count the length of the word in string

#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include<ctype.h>

void count_length_of_word(char * S)
{
    int length = 0; 
    int i = strlen(S)-1;

    printf("%d",i);
}
int main()
{
    char S[] = "Aritra ";
    count_length_of_word(S);
    return 0;
}